services:

  postgres:
    image: postgres:18.0
    env_file:
      - .env
    ports:
      - "5432:5432"

    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s